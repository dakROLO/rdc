const WORKDAYS_PER_YEAR=250;const SITE_VERSION="v1.1.6";const moneyFormatter=new Intl.NumberFormat("en-US",{style:"currency",currency:"USD",maximumFractionDigits:0});function clampNumber(value,min,max,fallback){const parsed=Number(value);if(!Number.isFinite(parsed))return fallback;return Math.min(Math.max(parsed,min),max)}function updateWorkflowEstimate(){const peopleInput=document.getElementById("people"),minutesInput=document.getElementById("minutes"),hourlyRateInput=document.getElementById("hourly-rate"),result=document.getElementById("annual-cost"),hoursResult=document.getElementById("annual-hours");if(!peopleInput||!minutesInput||!hourlyRateInput||!result||!hoursResult)return;const people=clampNumber(peopleInput.value,1,5000,10),minutes=clampNumber(minutesInput.value,1,480,15),hourlyRate=clampNumber(hourlyRateInput.value,1,1000,40),annualHours=people*(minutes/60)*WORKDAYS_PER_YEAR,annualCost=annualHours*hourlyRate;result.textContent=moneyFormatter.format(annualCost);hoursResult.textContent=`${Math.round(annualHours).toLocaleString("en-US")} hours`}function initializeCalculator(){const calculator=document.getElementById("workflow-calculator");if(!calculator)return;calculator.addEventListener("input",updateWorkflowEstimate);updateWorkflowEstimate()}function initializeSiteText(){const year=document.getElementById("year");if(year)year.textContent=new Date().getFullYear();const sessionHeading=document.querySelector("#sessions .section-heading h2");if(sessionHeading)sessionHeading.textContent="Book an outcome, not just a block of time.";const copyright=document.querySelector(".footer .footer-inner p");if(copyright&&!copyright.textContent.includes(SITE_VERSION))copyright.append(` · Site ${SITE_VERSION}`)}initializeCalculator();initializeSiteText();